[package]
name = "tli42"
version = "0.1.0"
edition = "2024"

[features]
default = ["rustyline"]
rustyline = ["dep:rustyline"]

[dependencies]
rustyline = { version = "17.0.2", optional = true, features = ["custom-bindings"] }
